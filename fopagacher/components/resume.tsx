/**
 * @file resume.tsx
 *
 * @brief Definition of the resume component used in the application.
 */

import React from "react";

/**
    * @function resume
    *
    * @brief Functional component representing the footer of the application.
    *
    * @returns {TSX.Element} - TSX element representing the footer.
 */
function resume() {
  return (
    <div>
      <h1>resume components</h1>
    </div>
  );
}

/**
    * @brief Export the resume component as the default export for use in other files.
  */
export default resume;
